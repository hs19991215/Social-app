workflows:
  react-native:
    name: React Native App
    environment:
      node: latest
    scripts:
      - yarn install
      - yarn android
      - yarn ios 
    artifacts:
      - android/app/build/outputs/**/**/*.apk
      - ios/build/Build/Products/**/*.app
