import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textContainer: {
    width: '90%',
    alignSelf: 'center',
    marginTop: 15,
    borderWidth: 1,
    borderRadius: 10,
  },
});
